version: "3.7"

services:
  db:
    image: postgres:11-alpine
    ports:
      - 5432:5432
    environment:
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=django_db
      - POSTGRES_USER=user_name